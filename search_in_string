string = 'Pushpak'
key = 'h'

def search(string,key):
    for i,j in enumerate(string):
        if j == key:
            return("alphabet is found at position",i)
    else:
        return(-1)

a=search(string,key)
print(a)