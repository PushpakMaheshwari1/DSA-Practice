array=[1,2,4,57,82,11]
key = 82

def search(array,key):

    for i,j in enumerate(array[1:5]):
        if j == key:
            return("Number is found at position",i)
    else:
        return(-1)

print(search(array,key))